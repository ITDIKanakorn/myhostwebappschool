<!-- Modal for Installment Payment -->
<div id="installmentModal" style="display:none;">
    <div style="position:fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(0, 0, 0, 0.7);"></div>
    <div
        style="position:fixed; top:50%; left:50%; transform:translate(-50%, -50%); background-color:white; padding:30px; border-radius:15px; width:600px; box-shadow:0 4px 20px rgba(0,0,0,0.2);">
        <h3 style="text-align:center; color:#333;">รายละเอียดการผ่อนชำระ</h3>
        <p style="text-align:center; color:#555;">แอดมินได้ตั้งค่าการผ่อนชำระสำหรับนักเรียนเรียบร้อยแล้ว:</p>

        <!-- แสดงข้อมูลการผ่อนชำระ -->
        <div id="installmentDetails" style="margin-bottom:20px;">
            <p style="font-weight:bold;">จำนวนงวดที่ต้องผ่อน: <strong id="installmentCount">4</strong> งวด</p>
            <p style="font-weight:bold;">จำนวนเงินทั้งหมดที่ต้องผ่อน: <strong id="totalAmount">4,000 บาท</strong></p>

            <!-- แสดงงวดที่เหลืออยู่ด้านบน -->
            <p id="remainingInstallments" style="font-weight:bold; margin-top:20px;">งวดที่เหลือ: <span id="remainingCount">4</span> งวด</p>
            <p style="font-weight:bold;">จำนวนเงินที่ต้องผ่อน: <strong id="remainingAmount">4,000 บาท</strong></p> <!-- จำนวนเงินที่ต้องผ่อน -->

            <!-- แสดงสถานะของแต่ละงวด -->
            <div id="installmentStatus" style="margin-top:10px;">
                <p>งวดที่ 1: <span id="installment1Status" style="color:orange;">ยังไม่ชำระเงิน</span></p>
                <button id="downloadReceipt1" style="padding:5px 10px; background-color:#2196F3; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(1)">ดาวน์โหลดใบชำระค่าลงทะเบียน</button>

                <p id="installment2" style="display:none;">งวดที่ 2: <span id="installment2Status" style="color:orange;">ยังไม่ชำระเงิน</span></p>
                <button id="downloadReceipt2" style="display:none; padding:5px 10px; background-color:#2196F3; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(2)">ดาวน์โหลดใบชำระค่าลงทะเบียน</button>

                <p id="installment3" style="display:none;">งวดที่ 3: <span id="installment3Status" style="color:orange;">ยังไม่ชำระเงิน</span></p>
                <button id="downloadReceipt3" style="display:none; padding:5px 10px; background-color:#2196F3; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(3)">ดาวน์โหลดใบชำระค่าลงทะเบียน</button>

                <p id="installment4" style="display:none;">งวดที่ 4: <span id="installment4Status" style="color:orange;">ยังไม่ชำระเงิน</span></p>
                <button id="downloadReceipt4" style="display:none; padding:5px 10px; background-color:#2196F3; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(4)">ดาวน์โหลดใบชำระค่าลงทะเบียน</button>
            </div>
        </div>

        <div style="text-align:center;">
            <button onclick="window.location.href='your_empty_page.html'" style="padding:10px 15px; background-color:#4CAF50; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;">ส่งหลักฐานการผ่อนชำระ</button>
            <button id="payInstallment2" style="display:none; padding:10px 15px; background-color:#4CAF50; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(2)">ชำระงวดที่ 2</button>
            <button id="payInstallment3" style="display:none; padding:10px 15px; background-color:#4CAF50; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(3)">ชำระงวดที่ 3</button>
            <button id="payInstallment4" style="display:none; padding:10px 15px; background-color:#4CAF50; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s;" onclick="payInstallment(4)">ชำระงวดที่ 4</button>
            <button onclick="closeModal()" style="padding:10px 15px; background-color:#f44336; color:white; border:none; border-radius:5px; cursor:pointer; transition: background-color 0.3s; margin-left: 10px;">ยกเลิก</button>
        </div>
    </div>
</div>



---------------------------------------------------------------------------------------------------------------------------------

// Total amount for installments
const totalInstallments = 4; // จำนวนงวดทั้งหมด
const amountPerInstallment = 1000; // จำนวนเงินต่อหนึ่งงวด
let remainingAmount = totalInstallments * amountPerInstallment; // จำนวนเงินที่คงเหลือ

// แสดงจำนวนเงินทั้งหมดใน modal
document.getElementById("totalAmount").innerText = remainingAmount + " บาท"; 
// แสดงจำนวนเงินที่ต้องผ่อนใน modal
document.getElementById("remainingAmount").innerText = remainingAmount + " บาท"; 

function payInstallment(installmentNumber) {
    // อัปเดตสถานะการชำระเงินตามหมายเลขงวด
    if (installmentNumber === 1) {
        document.getElementById("installment1Status").innerText = "ชำระแล้ว: 1,000 บาท🎉"; // อัปเดตสถานะงวดที่ 1
        document.getElementById("installment1Status").style.color = "green"; // เปลี่ยนสีเป็นเขียวเมื่อชำระเงินแล้ว
        document.getElementById("downloadReceipt1").style.display = "none"; // ซ่อนปุ่มดาวน์โหลดใบเสร็จสำหรับงวดที่ 1
        document.getElementById("installment2").style.display = "block"; // แสดงงวดที่ 2
        document.getElementById("downloadReceipt2").style.display = "block"; // แสดงปุ่มดาวน์โหลดสำหรับงวดที่ 2
        remainingAmount -= amountPerInstallment; // ลดจำนวนเงินที่ต้องผ่อน
        updateRemainingAmount(); // อัปเดตจำนวนเงินที่ต้องผ่อน
        updateRemainingInstallments(); // อัปเดตงวดที่เหลือ
    } else if (installmentNumber === 2) {
        document.getElementById("installment2Status").innerText = "ชำระแล้ว: 1,000 บาท🎉"; // อัปเดตสถานะงวดที่ 2
        document.getElementById("installment2Status").style.color = "green"; // เปลี่ยนสีเป็นเขียวเมื่อชำระเงินแล้ว
        document.getElementById("downloadReceipt2").style.display = "none"; // ซ่อนปุ่มดาวน์โหลดใบเสร็จสำหรับงวดที่ 2
        document.getElementById("installment3").style.display = "block"; // แสดงงวดที่ 3
        document.getElementById("downloadReceipt3").style.display = "block"; // แสดงปุ่มดาวน์โหลดสำหรับงวดที่ 3
        remainingAmount -= amountPerInstallment; // ลดจำนวนเงินที่ต้องผ่อน
        updateRemainingAmount(); // อัปเดตจำนวนเงินที่ต้องผ่อน
        updateRemainingInstallments(); // อัปเดตงวดที่เหลือ
    } else if (installmentNumber === 3) {
        document.getElementById("installment3Status").innerText = "ชำระแล้ว: 1,000 บาท🎉"; // อัปเดตสถานะงวดที่ 3
        document.getElementById("installment3Status").style.color = "green"; // เปลี่ยนสีเป็นเขียวเมื่อชำระเงินแล้ว
        document.getElementById("downloadReceipt3").style.display = "none"; // ซ่อนปุ่มดาวน์โหลดใบเสร็จสำหรับงวดที่ 3
        document.getElementById("installment4").style.display = "block"; // แสดงงวดที่ 4
        document.getElementById("downloadReceipt4").style.display = "block"; // แสดงปุ่มดาวน์โหลดสำหรับงวดที่ 4
        remainingAmount -= amountPerInstallment; // ลดจำนวนเงินที่ต้องผ่อน
        updateRemainingAmount(); // อัปเดตจำนวนเงินที่ต้องผ่อน
        updateRemainingInstallments(); // อัปเดตงวดที่เหลือ
    } else if (installmentNumber === 4) {
        document.getElementById("installment4Status").innerText = "ชำระแล้ว: 1,000 บาท🎉"; // อัปเดตสถานะงวดที่ 4
        document.getElementById("installment4Status").style.color = "green"; // เปลี่ยนสีเป็นเขียวเมื่อชำระเงินแล้ว
        document.getElementById("downloadReceipt4").style.display = "none"; // ซ่อนปุ่มดาวน์โหลดใบเสร็จสำหรับงวดที่ 4
        remainingAmount -= amountPerInstallment; // ลดจำนวนเงินที่ต้องผ่อน
        updateRemainingAmount(); // อัปเดตจำนวนเงินที่ต้องผ่อน
        updateRemainingInstallments(); // อัปเดตงวดที่เหลือ
    }
}

function updateRemainingAmount() {
    // อัปเดตจำนวนเงินที่ต้องผ่อน
    document.getElementById("remainingAmount").innerText = remainingAmount + " บาท"; 

    // ตรวจสอบว่าจำนวนเงินที่ต้องผ่อนเป็น 0 หรือไม่
    if (remainingAmount === 0) {
        document.getElementById("remainingAmount").innerHTML = "<span style='color: green;'>ผ่อนครบแล้ว 🎉</span>"; // แสดงข้อความว่าผ่อนครบแล้ว
    }
}

function updateRemainingInstallments() {
    const totalInstallments = 4; // จำนวนงวดทั้งหมด
    let paidInstallments = 0; // จำนวนงวดที่ชำระแล้ว

    // นับจำนวนงวดที่ชำระเงินแล้ว
    if (document.getElementById("installment1Status").innerText.includes("ชำระแล้ว")) paidInstallments++;
    if (document.getElementById("installment2Status").innerText.includes("ชำระแล้ว")) paidInstallments++;
    if (document.getElementById("installment3Status").innerText.includes("ชำระแล้ว")) paidInstallments++;
    if (document.getElementById("installment4Status").innerText.includes("ชำระแล้ว")) paidInstallments++;

    const remainingInstallments = totalInstallments - paidInstallments; // คำนวณจำนวนงวดที่เหลือ
    document.getElementById("remainingCount").innerText = remainingInstallments; // อัปเดตจำนวนงวดที่เหลือ

    // ตรวจสอบว่าจำนวนงวดที่เหลือเป็น 0 หรือไม่
    if (remainingInstallments === 0) {
        document.getElementById("remainingCount").innerHTML = "<span style='color: green;'>ผ่อนครบแล้ว 🎉</span>"; // แสดงข้อความว่าผ่อนครบแล้ว
    }
}

function closeModal() {
    document.getElementById("installmentModal").style.display = "none"; // ปิด modal
}

// เรียกใช้งานฟังก์ชันในที่ที่ต้องการ เช่น การเริ่มต้น
updateRemainingAmount();
updateRemainingInstallments();
